services:
  - type: web
    name: latex-pdf-translator
    runtime: python
    buildCommand: pip install .
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
    plan: free
